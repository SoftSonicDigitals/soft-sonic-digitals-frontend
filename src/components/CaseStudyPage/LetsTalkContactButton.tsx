"use client";
import React from "react";
import { PrimaryButton } from "../Reusable";

const LetsTalkContactButton = () => {
  return (
    <PrimaryButton
      title={"LET'S TALK"}
      className="px-12 py-3 hover:text-black transition-all duration-150"
    />
  );
};

export default LetsTalkContactButton;
