export type WeAreDifferentReasons = {
  title: string;
  reasons: {
    reason: string;
    description: string;
  }[];
};

export type ConcernsAndExpertise = {
  title: string;
  list: {
    title: string;
    description: string;
  }[];
};
